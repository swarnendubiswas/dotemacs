(
 (nil . (
         ;; (format-all-formatters . ("Python" yapf))
         ))
 (emacs-lisp-mode . (
                     ;; (eval . (format-all-mode 1)) ; Enable/disable a minor mode
                     ))
 )
